# OnLabel requires an Anthropic API key (the pipeline calls Claude).
# Local dev: copy to .env and fill in. Vercel: set as an Environment Variable.
# See DEPLOY.md.
ANTHROPIC_API_KEY=your-api-key-here
